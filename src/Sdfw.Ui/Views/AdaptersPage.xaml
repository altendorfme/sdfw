<Page
    x:Class="Sdfw.Ui.Views.AdaptersPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:vm="clr-namespace:Sdfw.Ui.ViewModels"
    xmlns:loc="clr-namespace:Sdfw.Ui.Localization"
    xmlns:converters="clr-namespace:Sdfw.Ui.Converters"
    d:DataContext="{d:DesignInstance vm:AdaptersViewModel}"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Loaded="OnLoaded">

    <Page.Resources>
        <converters:DnsListToDisplayConverter x:Key="DnsListToDisplayConverter" />
    </Page.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock
                Text="{loc:Localize Adapters_Title}"
                FontSize="24"
                FontWeight="SemiBold"
                Foreground="{DynamicResource TextFillColorPrimaryBrush}" />

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <ui:Button
                    Content="{loc:Localize Adapters_Save}"
                    Appearance="Primary"
                    Icon="{ui:SymbolIcon Save24}"
                    Command="{Binding SaveSelectionCommand}"
                    Margin="0,0,8,0" />
                <ui:Button
                    Icon="{ui:SymbolIcon ArrowSync24}"
                    Command="{Binding RefreshCommand}"
                    ToolTip="{loc:Localize Adapters_Refresh}" />
            </StackPanel>
        </Grid>

        <!-- Options -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,16">
            <ui:ToggleSwitch
                Content="{loc:Localize Adapters_ShowAll}"
                IsChecked="{Binding ShowAllAdapters}" />
            <ui:Button
                Content="{loc:Localize Adapters_SelectAll}"
                Command="{Binding SelectAllCommand}"
                Margin="16,0,8,0" />
            <ui:Button
                Content="{loc:Localize Adapters_ClearSelection}"
                Command="{Binding ClearSelectionCommand}" />
        </StackPanel>

        <!-- Adapters List -->
        <ui:ListView
            Grid.Row="2"
            ItemsSource="{Binding Adapters}">
            <ui:ListView.ItemContainerStyle>
                <Style TargetType="ui:ListViewItem" BasedOn="{StaticResource {x:Type ui:ListViewItem}}">
                    <Setter Property="Margin" Value="0" />
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="BorderThickness" Value="0" />
                </Style>
            </ui:ListView.ItemContainerStyle>
            <ui:ListView.ItemTemplate>
                <DataTemplate>
                    <ui:Card Margin="0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <!-- Selection Checkbox -->
                            <CheckBox
                                Grid.Column="0"
                                IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                Margin="0,0,16,0"
                                VerticalAlignment="Center" />

                            <!-- Adapter Info -->
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" />
                                <TextBlock
                                    Text="{Binding CurrentIpv4Dns, Converter={StaticResource DnsListToDisplayConverter}}"
                                    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                    FontSize="12"
                                    Margin="0,2,0,0" />
                                <TextBlock
                                    Text="{Binding Description}"
                                    Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                    FontSize="12" />
                                <TextBlock
                                    Text="{Binding AdapterType}"
                                    Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                                    FontSize="11" />
                            </StackPanel>
                        </Grid>
                    </ui:Card>
                </DataTemplate>
            </ui:ListView.ItemTemplate>
        </ui:ListView>

        <!-- Loading -->
        <ui:ProgressRing
            Grid.RowSpan="3"
            IsIndeterminate="True"
            Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}" />
    </Grid>
</Page>
